split -v
resize 100
screen
stuff "if [[ -d src && -f scripts/test ]]; then watchdir src/ scripts/test; fi^m"

focus
split
title status
screen
stuff "watch git status^m"
focus

screen
stuff "short_prompt^m^l"
